-- ============================================================
-- ForeverGuide / Init.lua
-- Last file in the TOC: every module and every compiled guide is
-- loaded by now, so start the lifecycle.
-- ============================================================

local _, ns = ...
ns.Boot()
